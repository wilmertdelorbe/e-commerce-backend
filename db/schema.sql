-- Remove the existing database if it exists
DROP DATABASE IF EXISTS ecommerce_backend_db;

-- Create a new database for our e-commerce backend system
CREATE DATABASE ecommerce_backend_db;

-- Note: After creating the database, you'll need to connect to it
-- before creating tables and adding data